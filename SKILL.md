---
name: github-trending-xhs-linear-dark-cards
description: 把 GitHub Trending Top5 生成成中文小红书风格卡片：内容先中文化，再生成 3:4 HTML 卡片并截图；最终封面与项目卡统一为暗色 Linear 风。
tags: [github, trending, xiaohongshu, html, screenshot, playwright, feishu, linear, dark]
---

# GitHub Trending Top5 → 中文小红书卡片（暗色 Linear 风定稿版）

适用场景：
- 要把 GitHub Trending 做成中文社交媒体盘点
- 需要稳定、可复现、不依赖模型直接在图中写中文字
- 想把封面和项目卡统一成同一套暗色产品风格
- 需要自动投递到飞书

## 最终定稿原则

### 1. 内容链路
1. 抓 GitHub Trending Top5
2. 每个项目单独做中文结构化总结
3. 生成 Markdown 长文
4. 生成固定尺寸 HTML 卡片
5. 用 Playwright 截图导出 PNG
6. 自动发到飞书

### 2. 中文内容结构
每个项目至少包含：
- 一句话看懂
- 这项目到底能干嘛（3 条）
- 适合拿来做什么（2 条）
- 一句话判断

其中：
- `适合拿来做什么` 必须任务导向，避免空话
- `一句话判断` 优先用这个句式：
  - `如果你的工作涉及到 XXX、XXX、XXX，那就适合打开看。`

### 3. 首页结构（定稿）
首页不要重复信息，只保留：
- badge：GitHub 今日 Top5
- 主标题
- 一句引导语
- 主面板：按 Top1~Top5 逐条列出“适合拿来做什么”
- 底部：一句话判断

注意：
- 删除首页“这项目到底能干嘛”区块，避免和用途重复
- 首页文案不要使用 `...` / `…` 截断
- 首页信息结构固定后，后续只改视觉，不重做信息结构

### 4. 视觉风格（最终采用）
整套卡片统一使用：
- 暗色 Linear 风
- 深色渐变背景
- 紫蓝色强调
- 半透明深色面板
- 大字号，优先手机阅读
- 封面和项目卡必须同风格，不允许封面与内容页割裂

### 5. 尺寸规范
- 画布：`1242 x 1660`
- 比例：`3:4`
- 不在图上展示任何“比例/分辨率/导出信息”元数据

### 6. 飞书投递
投递顺序建议：
1. 文本摘要
2. Markdown 文件
3. HTML 文件
4. 封面图 + 5 张项目卡

## 关键经验

### 不要做的事
- 不要只抓 Top1
- 不要让模型直接在图片中渲染大量中文
- 不要首页和项目卡用两套完全不同的视觉体系
- 不要写“适合谁看”这类空话
- 不要把首页做得信息重复又拥挤

### 应该做的事
- 每个 repo 单独中文化，避免大 JSON 失败
- 首页只承担总览和导读功能
- 风格评审时可以只输出首页候选版，不要每次整套都重做
- 一旦确定首页风格，要把项目卡同步统一

## 验收标准
满足以下几点视为合格：
1. 内容以中文为主，仅保留必要英文项目名/仓库名/URL/术语
2. 首页无重复信息、无省略号截断
3. 封面与项目卡为同一套暗色 Linear 风
4. PNG 尺寸固定为 `1242x1660`
5. 飞书能收到 md/html/png 全套产物

## 推荐仓库内容
如果要发布到 GitHub，至少包含：
- `README.md`（中文）
- `SKILL.md`
- `scripts/generate_trending_xhs.py`
- 示例输出截图（可选）
